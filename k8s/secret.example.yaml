# Template only -- fill in real values and apply as secret.yaml (gitignored),
# or better, skip this file entirely and run:
#
#   kubectl create secret generic slack-agent-secrets \
#     --namespace slack-agent \
#     --from-literal=SLACK_BOT_TOKEN=xoxb-... \
#     --from-literal=SLACK_APP_TOKEN=xapp-... \
#     --from-literal=VOYAGE_API_KEY=pa-... \
#     --from-literal=ANTHROPIC_API_KEY=sk-ant-...
#
# A plain Kubernetes Secret is only base64-encoded, not encrypted at rest by
# default on every cluster -- for real production, back this with your
# cluster's actual secrets manager (Sealed Secrets, External Secrets
# Operator, AWS/GCP secret manager sync, etc.) instead of raw Secret objects.
apiVersion: v1
kind: Secret
metadata:
  name: slack-agent-secrets
  namespace: slack-agent
type: Opaque
stringData:
  SLACK_BOT_TOKEN: "xoxb-your-token-here"
  SLACK_APP_TOKEN: "xapp-your-token-here"
  VOYAGE_API_KEY: "pa-your-key-here"
  ANTHROPIC_API_KEY: "sk-ant-your-key-here"
